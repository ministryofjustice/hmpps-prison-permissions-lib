import PrisonPermissionsService from './PrisonPermissionsService'

describe('PrisonPermissionsService', () => {
  it('Should be able to get permissions', () => {
    // new PrisonPermissionsService(null)
  })
})
